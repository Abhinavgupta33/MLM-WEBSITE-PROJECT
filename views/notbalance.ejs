<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Insufficient Balance</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f8f9fa;
    }
    .insufficient-container {
      background: #ffffff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      text-align: center;
      width: 400px;
    }
    .insufficient-container h1 {
      color: #dc3545;
      font-size: 24px;
      margin-bottom: 20px;
    }
    .insufficient-container p {
      color: #555;
      font-size: 16px;
      margin-bottom: 30px;
    }
    .insufficient-container button {
      padding: 10px 20px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      cursor: pointer;
      margin: 5px;
    }
    .insufficient-container button:hover {
      background-color: #0056b3;
    }
    .insufficient-container .deposit {
      background-color: #28a745;
    }
    .insufficient-container .deposit:hover {
      background-color: #218838;
    }
  </style>
</head>
<body>
  <div class="insufficient-container">
    <h1>Insufficient Balance</h1>
    <p>You do not have enough balance to complete this transaction. Please deposit funds or try a smaller amount.</p>
    <button onclick="goBack()">Go Back</button>
    <button class="deposit" onclick="depositFunds()">Deposit Funds</button>
  </div>

  <script>
    function goBack() {
      // Redirect to the previous page
      window.history.back();
    }

    function depositFunds() {
      // Redirect to the deposit funds page
      window.location.href = '/deposit';
    }
  </script>
</body>
</html>
