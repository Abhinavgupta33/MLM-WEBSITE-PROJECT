<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Cart | Modern Shop</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #4361ee;
            --primary-dark: #3a56d4;
            --primary-light: rgba(67, 97, 238, 0.1);
            --secondary: #3f37c9;
            --accent: #4895ef;
            --accent-light: rgba(72, 149, 239, 0.1);
            --danger: #f72585;
            --danger-light: rgba(247, 37, 133, 0.1);
            --success: #4cc9f0;
            --success-light: rgba(76, 201, 240, 0.1);
            --warning: #f8961e;
            --light: #f8f9fa;
            --dark: #212529;
            --gray: #6c757d;
            --light-gray: #e9ecef;
            --body-bg: #f5f7ff;
            --card-bg: #ffffff;
            --text-color: #212529;
            --text-light: #6c757d;
            --border-color: #dee2e6;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            --hover-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            --transition-slow: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .dark-mode {
            --primary: #4895ef;
            --primary-dark: #3f8ae0;
            --primary-light: rgba(72, 149, 239, 0.2);
            --secondary: #4361ee;
            --accent: #4cc9f0;
            --accent-light: rgba(76, 201, 240, 0.2);
            --danger: #f72585;
            --danger-light: rgba(247, 37, 133, 0.2);
            --success: #4cc9f0;
            --success-light: rgba(76, 201, 240, 0.2);
            --warning: #f8961e;
            --light: #343a40;
            --dark: #f8f9fa;
            --gray: #adb5bd;
            --light-gray: #495057;
            --body-bg: #121826;
            --card-bg: #1e293b;
            --text-color: #f8f9fa;
            --text-light: #adb5bd;
            --border-color: #334155;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            --hover-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: var(--body-bg);
            color: var(--text-color);
            line-height: 1.6;
            transition: var(--transition-slow);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
            position: relative;
        }

        /* Background animation elements */
        .bg-circle {
            position: fixed;
            border-radius: 50%;
            background: radial-gradient(circle, var(--primary-light), transparent 70%);
            filter: blur(60px);
            z-index: -1;
            animation: float 15s infinite ease-in-out;
            opacity: 0.7;
        }

        .bg-circle:nth-child(1) {
            width: 300px;
            height: 300px;
            top: 10%;
            left: 5%;
            animation-delay: 0s;
        }

        .bg-circle:nth-child(2) {
            width: 400px;
            height: 400px;
            bottom: 15%;
            right: 8%;
            animation-delay: 2s;
            background: radial-gradient(circle, var(--accent-light), transparent 70%);
        }

        .bg-circle:nth-child(3) {
            width: 250px;
            height: 250px;
            top: 50%;
            left: 30%;
            animation-delay: 4s;
            background: radial-gradient(circle, var(--danger-light), transparent 70%);
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0) translateX(0);
            }
            25% {
                transform: translateY(-20px) translateX(10px);
            }
            50% {
                transform: translateY(10px) translateX(-15px);
            }
            75% {
                transform: translateY(-15px) translateX(-10px);
            }
        }

        /* Main content */
        .main-content {
            flex: 1;
            padding: 2rem 0;
            position: relative;
            z-index: 1;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1.5rem;
        }

        /* Cart header */
        .cart-header {
            text-align: center;
            margin-bottom: 3rem;
            position: relative;
        }

        .cart-header h1 {
            font-size: 2.8rem;
            font-weight: 800;
            margin-bottom: 1rem;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            display: inline-block;
            position: relative;
            padding-bottom: 0.5rem;
        }

        .cart-header h1::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            border-radius: 2px;
        }

        .cart-header p {
            color: var(--text-light);
            font-size: 1.1rem;
            max-width: 600px;
            margin: 0 auto;
            line-height: 1.7;
        }

        /* Empty cart state */
        .empty-cart {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 4rem 2rem;
            border-radius: 16px;
            background-color: var(--card-bg);
            box-shadow: var(--shadow);
            transition: var(--transition-slow);
            position: relative;
            overflow: hidden;
            border: 1px solid var(--border-color);
        }

        .empty-cart::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, var(--primary), var(--accent));
        }

        .empty-cart-icon {
            font-size: 6rem;
            color: var(--primary);
            margin-bottom: 1.5rem;
            position: relative;
            animation: bounce 2s infinite, pulse 2s infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {transform: translateY(0);}
            40% {transform: translateY(-20px);}
            60% {transform: translateY(-10px);}
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }

        .empty-cart h2 {
            font-size: 2rem;
            margin-bottom: 1rem;
            color: var(--text-color);
            font-weight: 700;
        }

        .empty-cart p {
            color: var(--text-light);
            margin-bottom: 2rem;
            max-width: 500px;
            font-size: 1.1rem;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.75rem 1.75rem;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            transition: var(--transition);
            cursor: pointer;
            border: none;
            gap: 0.5rem;
            position: relative;
            overflow: hidden;
            z-index: 1;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            z-index: -1;
            opacity: 0;
            transition: var(--transition);
        }

        .btn-primary {
            background-color: var(--primary);
            color: white;
            box-shadow: 0 4px 20px rgba(67, 97, 238, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(67, 97, 238, 0.4);
        }

        .btn-primary:hover::before {
            opacity: 1;
        }

        .btn-outline {
            background-color: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
        }

        .btn-outline:hover {
            background-color: var(--primary);
            color: white;
            transform: translateY(-3px);
        }

        /* Cart with items */
        .cart-layout {
            display: grid;
            grid-template-columns: 1fr 350px;
            gap: 2rem;
            align-items: flex-start;
        }

        @media (max-width: 992px) {
            .cart-layout {
                grid-template-columns: 1fr;
            }
        }

        /* Cart items list */
        .cart-items {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .cart-item {
            display: flex;
            background-color: var(--card-bg);
            border-radius: 16px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
            position: relative;
            border: 1px solid var(--border-color);
        }

        .cart-item:hover {
            transform: translateY(-5px);
            box-shadow: var(--hover-shadow);
        }

        .cart-item-image-container {
            position: relative;
            overflow: hidden;
            width: 200px;
            height: 200px;
            flex-shrink: 0;
        }

        .cart-item-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition-slow);
        }

        .cart-item:hover .cart-item-image {
            transform: scale(1.05);
        }

        .cart-item-badge {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: var(--danger);
            color: white;
            padding: 0.25rem 0.5rem;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        @media (max-width: 768px) {
            .cart-item {
                flex-direction: column;
            }
            
            .cart-item-image-container {
                width: 100%;
                height: 220px;
            }
        }

        .cart-item-details {
            padding: 1.75rem;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .cart-item-title {
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            color: var(--text-color);
            transition: var(--transition);
        }

        .cart-item-title:hover {
            color: var(--primary);
        }

        .cart-item-meta {
            display: flex;
            gap: 1.5rem;
            margin-bottom: 0.75rem;
            color: var(--text-light);
            font-size: 0.95rem;
            flex-wrap: wrap;
        }

        .cart-item-meta span {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .cart-item-meta i {
            font-size: 0.8rem;
            color: var(--primary);
        }

        .cart-item-price {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .cart-item-price .original-price {
            font-size: 0.9rem;
            color: var(--text-light);
            text-decoration: line-through;
            margin-left: 0.5rem;
        }

        .cart-item-actions {
            display: flex;
            gap: 1rem;
            margin-top: auto;
            flex-wrap: wrap;
        }

        .quantity-selector {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background-color: var(--light-gray);
            border-radius: 12px;
            padding: 0.25rem;
        }

        .quantity-btn {
            width: 36px;
            height: 36px;
            border-radius: 10px;
            background-color: var(--light-gray);
            color: var(--text-color);
            border: none;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: var(--transition);
        }

        .quantity-btn:hover {
            background-color: var(--primary);
            color: white;
            transform: scale(1.1);
        }

        .quantity-input {
            width: 50px;
            text-align: center;
            padding: 0.5rem;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            background-color: transparent;
            color: var(--text-color);
            font-weight: 600;
            transition: var(--transition);
        }

        .quantity-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px var(--primary-light);
        }

        .remove-btn {
            background-color: transparent;
            color: var(--danger);
            border: 1px solid var(--danger);
            border-radius: 8px;
            padding: 0.5rem 1.25rem;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .remove-btn:hover {
            background-color: var(--danger);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(247, 37, 133, 0.2);
        }

        /* Cart summary */
        .cart-summary {
            background-color: var(--card-bg);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: var(--shadow);
            position: sticky;
            top: 1rem;
            border: 1px solid var(--border-color);
        }

        .summary-title {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            gap: 0.75rem;
            color: var(--primary);
        }

        .summary-title i {
            font-size: 1.2em;
        }

        .summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
            padding: 0.5rem 0;
            border-bottom: 1px dashed var(--border-color);
        }

        .summary-label {
            color: var(--text-light);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .summary-value {
            font-weight: 600;
        }

        .summary-total {
            font-size: 1.3rem;
            margin: 1.5rem 0;
            padding-top: 1rem;
            border-top: 2px solid var(--border-color);
        }

        .summary-total .summary-value {
            color: var(--primary);
            font-size: 1.5rem;
        }

        .checkout-btn {
            width: 100%;
            padding: 1rem;
            font-size: 1.1rem;
            background-color: var(--primary);
            color: white;
            border: none;
            border-radius: 12px;
            font-weight: 700;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            box-shadow: 0 4px 20px rgba(67, 97, 238, 0.3);
            margin-top: 1.5rem;
            position: relative;
            overflow: hidden;
        }

        .checkout-btn::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 5px;
            height: 5px;
            background: rgba(255, 255, 255, 0.5);
            opacity: 0;
            border-radius: 100%;
            transform: scale(1, 1) translate(-50%, -50%);
            transform-origin: 50% 50%;
        }

        .checkout-btn:focus:not(:active)::after {
            animation: ripple 1s ease-out;
        }

        @keyframes ripple {
            0% {
                transform: scale(0, 0);
                opacity: 0.5;
            }
            100% {
                transform: scale(20, 20);
                opacity: 0;
            }
        }

        .checkout-btn:hover {
            background-color: var(--primary-dark);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(67, 97, 238, 0.4);
        }

        .continue-shopping {
            display: block;
            text-align: center;
            margin-top: 1.5rem;
            color: var(--primary);
            font-weight: 600;
            text-decoration: none;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .continue-shopping:hover {
            text-decoration: underline;
            color: var(--primary-dark);
            transform: translateX(-5px);
        }

        /* Discount code input */
        .discount-code {
            margin: 1.5rem 0;
        }

        .discount-code label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--text-light);
            font-size: 0.9rem;
        }

        .discount-input-group {
            display: flex;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .discount-input {
            flex-grow: 1;
            padding: 0.75rem 1rem;
            border: none;
            background-color: var(--light-gray);
            color: var(--text-color);
            font-weight: 500;
        }

        .discount-input:focus {
            outline: none;
            background-color: var(--light);
        }

        .apply-discount {
            background-color: var(--primary);
            color: white;
            border: none;
            padding: 0 1.5rem;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
        }

        .apply-discount:hover {
            background-color: var(--primary-dark);
        }

        /* Floating action button */
        .fab {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 25px rgba(67, 97, 238, 0.3);
            cursor: pointer;
            z-index: 100;
            transition: var(--transition);
            animation: pulse 2s infinite;
            border: none;
        }

        .fab:hover {
            transform: scale(1.1) translateY(-5px);
            box-shadow: 0 8px 30px rgba(67, 97, 238, 0.4);
        }

        .fab-badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background-color: var(--danger);
            color: white;
            width: 24px;
            height: 24px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.75rem;
            font-weight: 700;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            animation: pulse 1.5s infinite;
        }

        /* Loading overlay */
        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.3s ease;
            backdrop-filter: blur(8px);
        }

        .loading-overlay.active {
            opacity: 1;
            pointer-events: all;
        }

        .spinner {
            width: 60px;
            height: 60px;
            border: 4px solid rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            border-top-color: var(--primary);
            animation: spin 1s linear infinite;
            position: relative;
        }

        .spinner::after {
            content: '';
            position: absolute;
            top: -4px;
            left: -4px;
            right: -4px;
            bottom: -4px;
            border: 4px solid transparent;
            border-radius: 50%;
            border-top-color: var(--accent);
            animation: spin 1.5s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .loading-text {
            color: white;
            margin-top: 1.5rem;
            font-weight: 500;
            text-align: center;
            max-width: 200px;
        }

        /* Toast notification */
        .toast {
            position: fixed;
            bottom: 2rem;
            left: 50%;
            transform: translateX(-50%) translateY(100px);
            background-color: var(--card-bg);
            color: var(--text-color);
            padding: 1rem 1.75rem;
            border-radius: 12px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
            display: flex;
            align-items: center;
            gap: 1rem;
            z-index: 1000;
            opacity: 0;
            transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            max-width: 90%;
            width: max-content;
            border-left: 5px solid var(--success);
        }

        .toast.show {
            transform: translateX(-50%) translateY(0);
            opacity: 1;
        }

        .toast-icon {
            font-size: 1.5rem;
        }

        .toast-success .toast-icon {
            color: var(--success);
        }

        .toast-error {
            border-left-color: var(--danger);
        }

        .toast-error .toast-icon {
            color: var(--danger);
        }

        .toast-content {
            display: flex;
            flex-direction: column;
        }

        .toast-title {
            font-weight: 700;
            margin-bottom: 0.25rem;
        }

        .toast-message {
            font-size: 0.9rem;
            color: var(--text-light);
        }

        .toast-close {
            margin-left: 1rem;
            background: none;
            border: none;
            color: var(--text-light);
            cursor: pointer;
            transition: var(--transition);
            font-size: 1.1rem;
        }

        .toast-close:hover {
            color: var(--danger);
        }

        /* Empty state animation */
        .empty-state-animation {
            width: 200px;
            height: 200px;
            margin: 0 auto 2rem;
            position: relative;
        }

        .empty-state-circle {
            position: absolute;
            border-radius: 50%;
            animation: float 6s infinite ease-in-out;
        }

        .empty-state-circle:nth-child(1) {
            width: 120px;
            height: 120px;
            background-color: var(--primary-light);
            top: 0;
            left: 40px;
            animation-delay: 0s;
        }

        .empty-state-circle:nth-child(2) {
            width: 80px;
            height: 80px;
            background-color: var(--accent-light);
            bottom: 20px;
            right: 30px;
            animation-delay: 1s;
        }

        .empty-state-circle:nth-child(3) {
            width: 60px;
            height: 60px;
            background-color: var(--danger-light);
            top: 60px;
            left: 20px;
            animation-delay: 2s;
        }

        /* Payment methods */
        .payment-methods {
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--border-color);
        }

        .payment-methods-title {
            font-size: 0.9rem;
            color: var(--text-light);
            margin-bottom: 1rem;
            text-align: center;
        }

        .payment-icons {
            display: flex;
            justify-content: center;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .payment-icon {
            width: 40px;
            height: 25px;
            object-fit: contain;
            filter: grayscale(100%);
            opacity: 0.7;
            transition: var(--transition);
        }

        .payment-icon:hover {
            filter: grayscale(0);
            opacity: 1;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .cart-header h1 {
                font-size: 2.2rem;
            }
            
            .empty-cart-icon {
                font-size: 5rem;
            }
            
            .empty-cart h2 {
                font-size: 1.6rem;
            }
            
            .cart-item-actions {
                flex-direction: column;
            }
            
            .quantity-selector {
                width: 100%;
                justify-content: space-between;
            }
            
            .remove-btn {
                width: 100%;
                justify-content: center;
            }

            .cart-summary {
                position: static;
            }
        }

        @media (max-width: 576px) {
            .cart-header h1 {
                font-size: 2rem;
            }
            
            .empty-cart-icon {
                font-size: 4rem;
            }
            
            .empty-cart h2 {
                font-size: 1.4rem;
            }

            .empty-cart {
                padding: 3rem 1.5rem;
            }

            .cart-item-details {
                padding: 1.25rem;
            }

            .cart-item-title {
                font-size: 1.15rem;
            }

            .cart-item-meta {
                gap: 0.75rem;
                font-size: 0.85rem;
            }

            .cart-summary {
                padding: 1.5rem;
            }
        }

        /* Animation classes */
        .animate-in {
            animation: fadeInUp 0.6s ease forwards;
            opacity: 0;
            animation-fill-mode: both;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .slide-in-left {
            animation: slideInLeft 0.6s ease forwards;
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .slide-in-right {
            animation: slideInRight 0.6s ease forwards;
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Item removal animation */
        .cart-item.removing {
            animation: slideOutRight 0.5s ease forwards;
        }

        @keyframes slideOutRight {
            to {
                opacity: 0;
                transform: translateX(100px);
                height: 0;
                margin-bottom: 0;
                padding: 0;
                border: 0;
            }
        }

        /* Hover effects */
        .hover-scale {
            transition: var(--transition);
        }

        .hover-scale:hover {
            transform: scale(1.02);
        }

        /* Tooltip */
        .tooltip {
            position: relative;
            display: inline-block;
        }

        .tooltip .tooltip-text {
            visibility: hidden;
            width: 120px;
            background-color: var(--dark);
            color: var(--light);
            text-align: center;
            border-radius: 6px;
            padding: 0.5rem;
            position: absolute;
            z-index: 1;
            bottom: 125%;
            left: 50%;
            transform: translateX(-50%);
            opacity: 0;
            transition: var(--transition);
            font-size: 0.8rem;
            font-weight: normal;
        }

        .tooltip .tooltip-text::after {
            content: "";
            position: absolute;
            top: 100%;
            left: 50%;
            margin-left: -5px;
            border-width: 5px;
            border-style: solid;
            border-color: var(--dark) transparent transparent transparent;
        }

        .tooltip:hover .tooltip-text {
            visibility: visible;
            opacity: 1;
        }

        /* Progress bar */
        .free-shipping-bar {
            margin: 1.5rem 0;
            background-color: var(--light-gray);
            border-radius: 10px;
            height: 10px;
            overflow: hidden;
            position: relative;
        }

        .free-shipping-progress {
            height: 100%;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            border-radius: 10px;
            transition: width 0.6s ease;
            width: 0;
        }

        .free-shipping-text {
            text-align: center;
            font-size: 0.85rem;
            color: var(--text-light);
            margin-top: 0.5rem;
        }

        .free-shipping-text .amount {
            color: var(--primary);
            font-weight: 600;
        }

        /* Recommended products */
        .recommended-products {
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid var(--border-color);
        }

        .recommended-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            text-align: center;
            color: var(--text-color);
        }

        .recommended-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 1.5rem;
        }

        .recommended-item {
            background-color: var(--card-bg);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
            border: 1px solid var(--border-color);
        }

        .recommended-item:hover {
            transform: translateY(-5px);
            box-shadow: var(--hover-shadow);
        }

        .recommended-image {
            width: 100%;
            height: 150px;
            object-fit: cover;
        }

        .recommended-details {
            padding: 1rem;
        }

        .recommended-name {
            font-size: 0.95rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: var(--text-color);
        }

        .recommended-price {
            font-size: 1rem;
            font-weight: 700;
            color: var(--primary);
        }

        .add-to-cart-btn {
            width: 100%;
            padding: 0.5rem;
            background-color: var(--primary);
            color: white;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            margin-top: 0.75rem;
            font-size: 0.9rem;
        }

        .add-to-cart-btn:hover {
            background-color: var(--primary-dark);
        }

        /* Responsive adjustments for recommended products */
        @media (max-width: 768px) {
            .recommended-grid {
                grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            }
        }

        /* Confetti effect */
        .confetti {
            position: fixed;
            width: 10px;
            height: 10px;
            background-color: var(--primary);
            opacity: 0;
            z-index: 999;
            animation: confetti-fall 5s linear forwards;
        }

        @keyframes confetti-fall {
            0% {
                transform: translateY(-100vh) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(100vh) rotate(720deg);
                opacity: 0;
            }
        }
    </style>
</head>
<body>
    <!-- Background animation elements -->
    <div class="bg-circle"></div>
    <div class="bg-circle"></div>
    <div class="bg-circle"></div>

    <%- include("../utils/header") %>
    <%- include("side") %>

    <main class="main-content">
        <div class="container">
            <div class="cart-header">
                <h1 class="animate__animated animate__fadeIn">Your Shopping Cart</h1>
                <% if (cart.length === 0) { %>
                    <p class="animate__animated animate__fadeIn animate__delay-1s">Your cart is currently empty. <a href="/viewproduct" style="color: var(--primary); text-decoration: none; font-weight: 600;">Start Shopping</a> to add amazing products!</p>
                <% } else { %>
                    <p class="animate__animated animate__fadeIn animate__delay-1s">Review your items and proceed to checkout</p>
                <% } %>
            </div>

            <% if (cart.length === 0) { %>
                <div class="empty-cart animate__animated animate__fadeIn">
                    <div class="empty-state-animation">
                        <div class="empty-state-circle"></div>
                        <div class="empty-state-circle"></div>
                        <div class="empty-state-circle"></div>
                        <div class="empty-cart-icon">
                            <i class="fas fa-shopping-cart"></i>
                        </div>
                    </div>
                    <h2>Your Cart is Empty</h2>
                    <p>Looks like you haven't added anything to your cart yet. Explore our collection and find something you love!</p>
                    <a href="/viewproduct" class="btn btn-primary">
                        <i class="fas fa-arrow-right"></i> Browse Products
                    </a>
                </div>

                <!-- Recommended products section -->
                <div class="recommended-products slide-in-left" style="animation-delay: 0.3s;">
                    <h3 class="recommended-title">You Might Also Like</h3>
                    <div class="recommended-grid">
                        <div class="recommended-item hover-scale">
                            <img src="https://images.unsplash.com/photo-1546868871-7041f2a55e12?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8MTh8fHByb2R1Y3R8ZW58MHx8MHx8fDA%3D&auto=format&fit=crop&w=500&q=60" alt="Product" class="recommended-image">
                            <div class="recommended-details">
                                <h4 class="recommended-name">Wireless Bluetooth Headphones</h4>
                                <p class="recommended-price">₹2,499</p>
                                <button class="add-to-cart-btn">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>
                        <div class="recommended-item hover-scale">
                            <img src="https://images.unsplash.com/photo-1523275335684-37898b6baf30?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8MTF8fHByb2R1Y3R8ZW58MHx8MHx8fDA%3D&auto=format&fit=crop&w=500&q=60" alt="Product" class="recommended-image">
                            <div class="recommended-details">
                                <h4 class="recommended-name">Smart Watch Pro</h4>
                                <p class="recommended-price">₹3,999</p>
                                <button class="add-to-cart-btn">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>
                        <div class="recommended-item hover-scale">
                            <img src="https://images.unsplash.com/photo-1505740420928-5e560c06d30e?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8M3x8cHJvZHVjdHxlbnwwfHwwfHx8MA%3D%3D&auto=format&fit=crop&w=500&q=60" alt="Product" class="recommended-image">
                            <div class="recommended-details">
                                <h4 class="recommended-name">Premium Headphones</h4>
                                <p class="recommended-price">₹1,799</p>
                                <button class="add-to-cart-btn">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>
                        <div class="recommended-item hover-scale">
                            <img src="https://images.unsplash.com/photo-1542291026-7eec264c27ff?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8NHx8cHJvZHVjdHxlbnwwfHwwfHx8MA%3D%3D&auto=format&fit=crop&w=500&q=60" alt="Product" class="recommended-image">
                            <div class="recommended-details">
                                <h4 class="recommended-name">Running Shoes</h4>
                                <p class="recommended-price">₹2,299</p>
                                <button class="add-to-cart-btn">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <div class="cart-layout">
                    <div class="cart-items">
                        <% cart.forEach((item, index) => { %>
                            <div class="cart-item animate-in" style="animation-delay: <%= index * 0.1 %>s;">
                                <div class="cart-item-image-container">
                                    <img src="<%= item.product_image %>" alt="<%= item.product_name %>" class="cart-item-image">
                                    <% if (item.discount) { %>
                                        <span class="cart-item-badge"><%= item.discount %>% OFF</span>
                                    <% } %>
                                </div>
                                <div class="cart-item-details">
                                    <h3 class="cart-item-title"><%= item.product_name %></h3>
                                    <div class="cart-item-meta">
                                        <span><i class="fas fa-palette"></i> Color: <%= item.color %></span>
                                        <span><i class="fas fa-box"></i> In Stock</span>
                                        <span><i class="fas fa-truck"></i> Free Shipping</span>
                                    </div>
                                    <p class="cart-item-price">
                                        ₹<%= (item.product_price * item.product_quantity).toFixed(2) %>
                                        <% if (item.original_price) { %>
                                            <span class="original-price">₹<%= (item.original_price * item.product_quantity).toFixed(2) %></span>
                                        <% } %>
                                    </p>
                                    <div class="cart-item-actions">
                                        <div class="quantity-selector">
                                            <button class="quantity-btn minus tooltip" data-id="<%= item.product_name %>">
                                                <i class="fas fa-minus"></i>
                                                <span class="tooltip-text">Decrease quantity</span>
                                            </button>
                                            <form action="/updatequantity" method="post" class="quantity-form">
                                                <input type="hidden" name="productId" value="<%= item.product_name %>">
                                                <input type="number" name="quantity" class="quantity-input" 
                                                    value="<%= item.product_quantity %>" min="1" max="1000">
                                            </form>
                                            <button class="quantity-btn plus tooltip" data-id="<%= item.product_name %>">
                                                <i class="fas fa-plus"></i>
                                                <span class="tooltip-text">Increase quantity</span>
                                            </button>
                                        </div>
                                        <form action="/removeproduct" method="post" class="remove-form">
                                            <input type="hidden" name="id" value="<%= item.product_name %>">
                                            <button type="submit" class="remove-btn">
                                                <i class="fas fa-trash-alt"></i> Remove
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>

                    <div class="cart-summary slide-in-right" style="animation-delay: 0.2s;">
                        <h3 class="summary-title">
                            <i class="fas fa-receipt"></i> Order Summary
                        </h3>
                        
                        <!-- Free shipping progress bar -->
                        <div class="free-shipping-bar">
                            <div class="free-shipping-progress" style="width: 65%"></div>
                        </div>
                        <div class="free-shipping-text">
                            Add <span class="amount">₹500</span> more to get free shipping!
                        </div>

                        <div class="summary-row">
                            <span class="summary-label"><i class="fas fa-boxes"></i> Subtotal (<%= cart.reduce((total, item) => total + item.product_quantity, 0) %> items)</span>
                            <span class="summary-value">₹<%= cart.reduce((total, item) => total + item.product_price * item.product_quantity, 0).toFixed(2) %></span>
                        </div>
                        <div class="summary-row">
                            <span class="summary-label"><i class="fas fa-truck"></i> Shipping</span>
                            <span class="summary-value">Free</span>
                        </div>
                        <div class="summary-row">
                            <span class="summary-label"><i class="fas fa-percentage"></i> Discount</span>
                            <span class="summary-value">-₹0.00</span>
                        </div>
                        <div class="summary-row">
                            <span class="summary-label"><i class="fas fa-receipt"></i> Tax</span>
                            <span class="summary-value">₹0.00</span>
                        </div>
                        <div class="summary-row summary-total">
                            <span class="summary-label">Total</span>
                            <span class="summary-value">₹<%= cart.reduce((total, item) => total + item.product_price * item.product_quantity, 0).toFixed(2) %></span>
                        </div>

                        <!-- Discount code input -->
                        <div class="discount-code">
                            <label for="discount-code"><i class="fas fa-tag"></i> Discount Code</label>
                            <div class="discount-input-group">
                                <input type="text" id="discount-code" class="discount-input" placeholder="Enter promo code">
                                <button type="button" class="apply-discount">Apply</button>
                            </div>
                        </div>

                        <form action="/buynow" method="get" class="checkout-form">
                            <input type="hidden" name="hello" value="<%= cart.reduce((total, item) => total + item.product_price * item.product_quantity, 0) %>">
                            <button type="submit" class="checkout-btn">
                                <i class="fas fa-credit-card"></i> Proceed to Checkout
                            </button>
                        </form>
                        <a href="/viewproduct" class="continue-shopping">
                            <i class="fas fa-arrow-left"></i> Continue Shopping
                        </a>

                        <!-- Payment methods -->
                        <div class="payment-methods">
                            <div class="payment-methods-title">We Accept</div>
                            <div class="payment-icons">
                                <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/visa/visa-original.svg" class="payment-icon" alt="Visa">
                                <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/mastercard/mastercard-original.svg" class="payment-icon" alt="Mastercard">
                                <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/paypal/paypal-original.svg" class="payment-icon" alt="PayPal">
                                <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/apple/apple-original.svg" class="payment-icon" alt="Apple Pay">
                                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/b/b5/Google_Pay_2020.svg/512px-Google_Pay_2020.svg.png" class="payment-icon" alt="Google Pay">
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </main>

    <!-- Floating action button -->
    <button class="fab" id="cartFab" aria-label="Scroll to top">
        <i class="fas fa-shopping-cart"></i>
        <% if (cart.length > 0) { %>
            <span class="fab-badge"><%= cart.length %></span>
        <% } %>
    </button>

    <!-- Loading overlay -->
    <div class="loading-overlay" id="loadingOverlay">
        <div class="spinner"></div>
        <div class="loading-text">Processing your request...</div>
    </div>

    <!-- Toast notification -->
    <div class="toast" id="toast">
        <i class="fas fa-check-circle toast-icon"></i>
        <div class="toast-content">
            <div class="toast-title">Success</div>
            <div class="toast-message" id="toastMessage">Item removed from cart</div>
        </div>
        <button class="toast-close" id="toastClose">&times;</button>
    </div>

    <%- include("../utils/footer") %>

    <script>
        // Dark mode synchronization
        function syncDarkMode() {
            const darkModeEnabled = localStorage.getItem('darkMode') === 'enabled';
            if (darkModeEnabled) {
                document.body.classList.add('dark-mode');
            } else {
                document.body.classList.remove('dark-mode');
            }
        }

        // Initialize dark mode
        syncDarkMode();

        // Listen for dark mode changes from header
        window.addEventListener('darkModeChanged', syncDarkMode);

        // Quantity controls
        document.querySelectorAll('.quantity-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                const form = this.closest('.quantity-selector').querySelector('.quantity-form');
                const input = form.querySelector('.quantity-input');
                const currentValue = parseInt(input.value);
                const productId = this.dataset.id;
                
                if (this.classList.contains('plus')) {
                    input.value = currentValue + 1;
                    showToast('Quantity increased', 'success');
                } else if (this.classList.contains('minus') && currentValue > 1) {
                    input.value = currentValue - 1;
                    showToast('Quantity decreased', 'success');
                }
                
                // Submit the form
                showLoading();
                setTimeout(() => {
                    form.submit();
                }, 500);
            });
        });

        // Quantity input change
        document.querySelectorAll('.quantity-input').forEach(input => {
            input.addEventListener('change', function() {
                if (this.value < 1) this.value = 1;
                if (this.value > 1000) this.value = 1000;
                
                showLoading();
                setTimeout(() => {
                    this.closest('.quantity-form').submit();
                }, 500);
            });
        });

        // Remove item confirmation
        document.querySelectorAll('.remove-form').forEach(form => {
            form.addEventListener('submit', function(e) {
                e.preventDefault();
                
                const item = this.closest('.cart-item');
                item.classList.add('removing');
                
                showLoading();
                setTimeout(() => {
                    this.submit();
                    showToast('Item removed from cart', 'success');
                    createConfetti();
                }, 800);
            });
        });

        // Checkout button
        document.querySelector('.checkout-form')?.addEventListener('submit', function(e) {
            showLoading();
            showToast('Processing your order', 'success');
        });

        // Apply discount code
        document.querySelector('.apply-discount')?.addEventListener('click', function() {
            const codeInput = document.getElementById('discount-code');
            if (codeInput.value.trim() === '') {
                showToast('Please enter a discount code', 'error');
                return;
            }
            
            showToast('Applying discount code...', 'success');
            // Here you would typically send the code to your server for validation
            setTimeout(() => {
                showToast('Discount code applied successfully!', 'success');
            }, 1500);
        });

        // Add to cart buttons for recommended products
        document.querySelectorAll('.add-to-cart-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                const productCard = this.closest('.recommended-item');
                productCard.style.transform = 'scale(0.95)';
                
                showToast('Product added to cart!', 'success');
                createConfetti();
                
                setTimeout(() => {
                    productCard.style.transform = '';
                    // Here you would typically redirect to the cart or refresh the page
                    window.location.href = '/viewproduct';
                }, 500);
            });
        });

        // Show loading overlay
        function showLoading() {
            document.getElementById('loadingOverlay').classList.add('active');
        }

        // Hide loading overlay
        function hideLoading() {
            document.getElementById('loadingOverlay').classList.remove('active');
        }

        // Show toast notification
        function showToast(message, type = 'success') {
            const toast = document.getElementById('toast');
            const toastMessage = document.getElementById('toastMessage');
            
            toast.className = `toast toast-${type}`;
            toastMessage.textContent = message;
            
            if (type === 'error') {
                toast.querySelector('.toast-icon').className = 'fas fa-exclamation-circle toast-icon';
                toast.querySelector('.toast-title').textContent = 'Error';
            } else {
                toast.querySelector('.toast-icon').className = 'fas fa-check-circle toast-icon';
                toast.querySelector('.toast-title').textContent = 'Success';
            }
            
            toast.classList.add('show');
            
            // Auto-hide after 3 seconds
            setTimeout(() => {
                toast.classList.remove('show');
            }, 3000);
        }

        // Close toast manually
        document.getElementById('toastClose').addEventListener('click', function() {
            document.getElementById('toast').classList.remove('show');
        });

        // Floating action button
        const fab = document.getElementById('cartFab');
        
        window.addEventListener('scroll', function() {
            if (window.scrollY > 300) {
                fab.style.opacity = '1';
                fab.style.pointerEvents = 'all';
            } else {
                fab.style.opacity = '0';
                fab.style.pointerEvents = 'none';
            }
        });

        fab.addEventListener('click', function() {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });

        // Create confetti effect
        function createConfetti() {
            const colors = ['#4361ee', '#4895ef', '#4cc9f0', '#f72585', '#f8961e'];
            
            for (let i = 0; i < 50; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.left = Math.random() * 100 + 'vw';
                confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                confetti.style.width = Math.random() * 10 + 5 + 'px';
                confetti.style.height = Math.random() * 10 + 5 + 'px';
                confetti.style.animationDuration = Math.random() * 3 + 2 + 's';
                document.body.appendChild(confetti);
                
                // Remove confetti after animation
                setTimeout(() => {
                    confetti.remove();
                }, 5000);
            }
        }

        // Animate elements when they come into view
        const animateOnScroll = () => {
            const elements = document.querySelectorAll('.animate-in, .slide-in-left, .slide-in-right');
            
            elements.forEach(element => {
                const elementPosition = element.getBoundingClientRect().top;
                const screenPosition = window.innerHeight / 1.2;
                
                if (elementPosition < screenPosition) {
                    element.style.animationPlayState = 'running';
                }
            });
        };

        window.addEventListener('scroll', animateOnScroll);
        animateOnScroll(); // Run once on page load

        // Ripple effect for buttons
        document.querySelectorAll('.checkout-btn, .btn-primary').forEach(button => {
            button.addEventListener('click', function(e) {
                const rect = this.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                
                const ripple = document.createElement('span');
                ripple.style.left = x + 'px';
                ripple.style.top = y + 'px';
                ripple.classList.add('ripple-effect');
                
                this.appendChild(ripple);
                
                setTimeout(() => {
                    ripple.remove();
                }, 1000);
            });
        });

        // Initialize tooltips
        document.querySelectorAll('.tooltip').forEach(tooltip => {
            tooltip.addEventListener('mouseenter', function() {
                const tooltipText = this.querySelector('.tooltip-text');
                tooltipText.style.visibility = 'visible';
                tooltipText.style.opacity = '1';
            });
            
            tooltip.addEventListener('mouseleave', function() {
                const tooltipText = this.querySelector('.tooltip-text');
                tooltipText.style.visibility = 'hidden';
                tooltipText.style.opacity = '0';
            });
        });

        // Simulate free shipping progress (in a real app, this would come from your backend)
        setTimeout(() => {
            const progressBar = document.querySelector('.free-shipping-progress');
            if (progressBar) {
                progressBar.style.width = '65%';
            }
        }, 500);
    </script>
</body>
</html>